<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/bg_main_view"
    android:fitsSystemWindows="true"
    android:orientation="vertical" >
    
    <include layout="@layout/layout_view_head"/>
	
    <ListView
    android:id="@+id/lv_class"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:cacheColorHint="#00000000"
    android:layout_marginTop="20dp"
    android:divider="@color/bg_line"
    android:dividerHeight="0.5dp"
    android:background="@drawable/layer_record_bg"
    android:listSelector="@drawable/selector_translucent_bg"
    />
</LinearLayout>
